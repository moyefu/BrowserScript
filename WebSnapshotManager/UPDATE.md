# 更新日志 (Changelog)

本项目遵循 [Semantic Versioning](https://semver.org/lang/zh-CN/) 语义化版本规范。

---

## [1.5.0] - 2026-09-18

### 🍪 Cookie 跨浏览器一致性与精准清理重构
- **根因彻底修复：抹平跨浏览器 `.` 带点与不带点差异**：
  - Chromium 底层严格区分 Host-Only Cookie（`hostOnly: true`，无前导点）与 Domain Cookie（`hostOnly: false`，带前导点 `.domain`）；
  - 修复 `restoreSession` 盲目传递 `domain` 导致 Host-Only Cookie 被污染为带点 Domain Cookie 的缺陷；
  - 引入「标准化元数据方案」：捕获时精确记录 `hostOnly`、`partitionKey`（CHIPS 分区 Cookie）、`sameSite`，并将 `domain` 规范化为无前导点的标准域名；
  - 恢复时严格分流：Host-Only Cookie 绝不传 `domain`，依赖 URL 绑定为纯粹的主机 Cookie；Domain Cookie 规范传参保持子域共享，完美抹平 Chrome/Edge/Firefox/ScriptCat/Tampermonkey 差异；
  - 读时向下兼容历史快照：自动推断旧数据格式并平滑兼容升级。
- **精准动态合成 URL 清除引擎：彻底解决带点与特定路径 Cookie 清理残留**：
  - 针对每一个待删除的 Cookie，根据其 `secure`、`cleanDomain`、`path` 动态合成绝对精准的目标匹配 URL，解决 Tampermonkey/Chromium `chrome.cookies.remove` 因 URL 路径/协议不匹配而静默拒绝删除的问题；
  - 清除阶段父域查询剥离前导点，解决 Chromium 传入带点 domain 导致子域搜索失效的问题；
  - 交互式清除作用域弹窗：点击“清空当前网站快照数据”弹出范围选择抽屉，默认「彻底清理生效域」（同时清理父级带点泛域，确保彻底退出登录态），支持勾选「仅清理当前精准子域名」。

### ⚡ 全局运行性能与非目标站点极致静默
- **非目标站点 0 开销静默**：
  - 白名单模式未命中或黑名单模式命中时，入口立即短路 `return`；
  - 取消全局非激活环境的 `Element.prototype.innerHTML` 原型链劫持，避免 SPA 框架（Vue/React）兼容性干扰与全网页性能损耗；
  - 延迟初始化：仅保留轻量油猴菜单，只有当用户从菜单临时/永久激活插件时才按需挂载。

### 🗜️ 存储与 Gist 数据原生无损压缩 (CompressionStream)
- **基于原生 CompressionStream 的透明无损压缩**：
  - 引入 `CompressionEngine`，利用现代浏览器原生内置的 `CompressionStream('deflate-raw')`；
  - 当快照体积超过 4KB 时透明无损压缩并 Base64 编码，快照存储体积缩减 70%~90%，彻底解除油猴本地存储上限与 Gist 同步大文件限制。

### 🏗️ 零依赖源码模块化工程化与极简构建
- **源码解耦拆分至 `src/` 目录**：
  - `meta.js`: UserScript 头、UserConfig 与 Trusted Types 策略
  - `theme.js`: ThemeEngine 主题引擎与预设
  - `compress.js`: CompressionEngine 原生压缩解压引擎
  - `crypto.js`: CryptoEngine AES-GCM 256 加密与多版本回退解密
  - `session.js`: SessionManager 会话与 Cookie 捕获/恢复/精准清除
  - `db.js`: DB 多域名快照存储与墓碑同步
  - `gist.js`: GistSyncEngine GitHub Gist 云同步
  - `ui.js`: LSM_UI 悬浮球、设置窗口、主题编辑器、分片 QR 码
  - `main.js`: 站点过滤短路拦截与生命周期启动器
- **极简零依赖构建**：
  - 根目录提供 `build.js`，运行 `node build.js` 或 `npm run build` 8ms 极速完成合并打包与语法校验，生成单文件 `index.user.js`。

---

## [1.4.5] - 2026-09-10

### 📍 悬浮球 / 管理窗口视口百分比位置记忆
- **位置按视口分数持久化（0~1）**：
  - 悬浮球与「快照管理」主窗口的拖拽位置改为按视口宽高分数（`fx` / `fy`，取值 0~1）存储，而非固定像素坐标；
  - 缩放窗口、调整浏览器缩放比例或刷新页面后，按当前视口等比还原位置，避免旧像素坐标导致的过度位移；
- **视口 Resize 等比重算**：
  - 监听 `resize` 事件，实时按已存分数重新计算悬浮球与管理窗口位置并做边界钳制，防止溢出屏幕；
- **旧版数据平滑兼容迁移**：
  - 自动识别旧版像素值（`x` / `y`）存储并迁移为视口分数，保证历史记忆位置不丢失；
- **修复百分比换算缺陷**：
  - 修正恢复/重算环节分数与像素换算重复除以 100 的问题，修复刷新后位置被错误钳到左上角 10px 的缺陷。

---

## [1.4.4] - 2026-09-02

### ☁️ GitHub Gist 云同步高容错容灾与智能恢复引擎
- **大文件截断自动检测与 Raw URL 兜底拉取**：
  - 深度适配 GitHub Gist 对大体积文件的截断限制（`truncated: true` 或 content 为空），自动解析并平滑拉取 `raw_url` 获取完整快照数据；
- **智能 JSON 解析与语法级容错恢复**：
  - 自动清理 UTF-8 BOM 头与末尾非法多余逗号；
  - 内置基于大括号堆栈分析的容错分块扫描机制（`recoverCorruptedGistJson`），即使云端快照 JSON 发生局部损坏，仍能完整提取并恢复未损坏的快照记录、自定义主题与墓碑数据；
- **细粒度数据清洗与健康校验**：
  - 引入单条快照记录健康度校验（`isValidRecord`），对快照载荷、加密密文结构进行严格把关；
  - 对墓碑映射表与主题配置表进行全量清洗校验，自动剔除损坏坏块，并在同步状态卡片中明确反馈已过滤忽略的损坏条目数，防止脏数据蔓延。

### ⏱️ 全新空闲自动同步机制（防打扰与无感静默同步）
- **从“即时触发”升级为“空闲静默同步”**：
  - 彻底解决用户在操作网页或使用脚本管理面板时频繁触发后台同步导致的性能波动与网络并发冲突；
- **全域活跃度检测与插件使用状态感知**：
  - 智能监听主文档及 Shadow DOM 内部的鼠标移动、点击、键盘按键、触摸交互；
  - 感知插件主窗口与各设置抽屉的开启状态，确保仅在用户连续达到设定时长无任何操作且插件处于空闲时，方静默触发增量同步；
- **空闲时长灵活配置与防并发互斥锁**：
  - 云同步设置面板新增 **「空闲自动同步时长（分钟）」** 自定义配置（支持 1~1440 分钟自由调节，默认 5 分钟）；
  - 引入同步互斥锁机制，防止多标签页或定时器重叠触发并发同步。

### 📋 Gist 绑定交互重构与搜索模态窗内嵌创建
- **主面板按钮精简与一致性优化**：
  - 云同步设置面板 Gist ID 区域简化为「输入与选择」，提供专属 **「📋 选择 Gist」** 按钮，点击一键拉取并打开 Gist 搜索选择模态窗；
- **选择弹窗标题行内嵌「🚀 自动创建 Gist」**：
  - 模态弹窗宽度拓宽至 520px，在弹窗顶部标题行（`选择已有 Gist 进行绑定 (N)`）旁直接集成自动创建快捷按钮，支持随时一键创建 Secret Gist 并即时绑定；
- **空记录与搜索无结果智能引导**：
  - 当 GitHub 账号暂无 Gist 记录时，列表区展示友好提示并提供「🚀 立即自动创建专属 Gist」按钮；
  - 当搜索关键词无匹配项时，展示「🚀 自动新建专属 Gist」引导按钮，点击全自动创建、回填 ID、保存配置并激活云同步。

---

## [1.4.3] - 2026-08-27

### ☁️ 自定义主题深度集成 GitHub Gist 云同步
- **自定义主题双向增量同步与合并**：
  - 云同步协议扩展支持 `custom_themes` 与主题墓碑（`theme_tombstones`），仅同步用户个性化新建与导入的自定义主题，官方内置预设保持纯净；
  - 增量合并引擎支持版本时间戳判断（`updatedAt` 择新）与 30 天墓碑清理机制，保证多设备间自定义主题增删改的精准同步；
- **主题操作全链路自动同步触发**：
  - 新建、修改、删除或导入自定义主题时，自动触发展期增量同步调度（`scheduleAutoSync`）；
  - 云同步完成后无缝刷新全局主题列表、顶栏快捷菜单并即时应用生效。

### 🎨 主题选项响应式截断控制与修改/新增自定义主题拆分
- **主题卡片文本长度截断与弹性自适应**：
  - 为主题网格单元与标题设置 `min-width: 0`、`text-overflow: ellipsis` 与 `flex-shrink: 0`，防止长文本撑开网格破坏布局；
  - 增强当前激活主题卡片（`theme-current-card`）标题与状态徽章的截断保护；
- **自适应单列自然换行（一行一个，无需维护多套样式）**：
  - 采用 `grid-template-columns: repeat(auto-fill, minmax(190px, 1fr))` 纯布局自适应，当窗口或可用宽度不足时自然换行为单列展示（一行一个），无需侵入修改移动端规则或做两套分支处理；
- **“自定义配色微调与新建”拆分为独立“修改”与“新增”**：
  - **✏️ 修改当前配色**：快速修改当前主题名称与 Accent / Paper / Header 配色，若为自定义主题直接持久化覆盖更新，若为官方预设则生成独立副本保存并应用；
  - **➕ 新建自定义主题**：基于当前主题为模板，快速创建全新自定义主题，赋予新主题独立唯一 ID 并即时生效；
  - 配套优化主题编辑器标题指示、表单预填充、按钮激活高亮以及「✕ 收起」快捷交互。

---

## [1.4.2] - 2026-08-27

### 🛡️ Trusted Types 策略引擎与严格 CSP 全环境深度兼容
- **多策略智能白名单适配 (`Trusted Types`)**：
  - 自动轮询创建与探测常见策略标识（`default`, `snapshotPolicy`, `webSnapshotManager`, `goog#html`, `dompurify`, `angular#unsafe-bypass`, `nextjs#html` 等），全面兼容 `window.trustedTypes` 与 `unsafeWindow.trustedTypes`；
- **安全渲染与多层降级引擎 (`setSafeInnerHTML`)**：
  - 实现了三级 DOM 安全注入降级方案（`innerHTML(TrustedHTML)` $\to$ `createContextualFragment` $\to$ `DOMParser` $\to$ `replaceChildren`），确保无论页面 CSP 策略多严苛均能 100% 成功渲染；
- **全局 `Element.prototype.innerHTML` 拦截器**：
  - 在 Userscript 上下文自动重写 `innerHTML` Setter，透明将原生 HTML 字符串转译为 `TrustedHTML`，防止第三方或内部调用直接触发拦截；
- **主界面显式安全注入**：
  - 针对 `initApp` 根节点、弹窗与各卡片渲染全面接入安全注入体系，彻底消除 `This document requires 'TrustedHTML' assignment` 报错。

---

## [1.4.0] - 2026-08-27

### 🎨 主题组合封装 JSON、自定义主题导入/导出与 Yohaku 初始默认配置
- **结构化 JSON 主题系统 (`ThemeEngine`)**：
  - 将所有界面样式与配色解耦封装为标准化 `LSM_THEME` JSON 格式（包含元数据、Tokens 字典、材质底色、边框、阴影、圆角及字体规范）；
  - **默认初始配置**：内置并将 **Yohaku (余白)** 作为默认初始化主题，同时提供 5 套精美官方预设（🌸 余白 / 🔷 经典科技蓝 / 🌑 曜石暗夜 / 🌸 春櫻粉紫 / 🍵 宇治抹茶）；
  - **动态 CSS 变量注入**：Shadow DOM 样式全面重构为 `--lsm-*` CSS 变量，切换主题 **0 延迟、0 闪烁、0 刷新** 实时热生效。
- **自定义主题导入 / 导出与流转**：
  - **📤 导出主题 (JSON)**：一键将当前主题打包导出为 `.json` 配置文件或直接复制格式化 JSON 到剪贴板；
  - **📥 导入主题 (JSON)**：支持选取本地 `.json` 主题文件或直接粘贴 JSON 字符串，自动校验 Schema、智能容错补全缺失字段并即时激活；
  - **✏️ 配色微调与新建**：内置可视化颜色选择器，支持实时微调 Accent、Paper、Header 等配色并保存为新的专属主题；
  - **🔄 一键恢复默认**：任何时候均可一键重置为 Yohaku (余白) 官方默认配置；
  - **内置删除防护**：官方预设受系统保护禁止误删，用户自定义主题支持一键删除与回退。
- **专属「🎨 主题风格设置」抽屉与扩展菜单联动**：
  - 工具栏下拉菜单与油猴/ScriptCat 扩展菜单新增 **「🎨 主题风格设置」** 快捷入口；
  - 抽屉内置当前主题色板预览（Color Swatches Palette）与多主题网格卡片选择器。

---

## [1.3.0] - 2026-08-27

### ☁️ GitHub Gist 云同步与三大核心数据同步策略升级
- **墓碑机制 (Tombstones) 彻底解决删除快照后被云端复活问题（问题 1）**：
  - 本地删除快照时自动生成带时间戳的墓碑标记（`LSM_TOMBSTONES`）；
  - 双向增量同步时自动合并两端墓碑列表，依据 `deletedAt >= updatedAt` 精准在云端与本地同步剔除已删除快照，杜绝旧快照被反向恢复；
  - 内置 30 天墓碑自动垃圾回收（GC）机制，防止元数据无限占用存储。
- **全自动双向互补增量合并（纯净无损同步）**：
  - 移除所有单向强制覆盖（强制推送/强制拉取），全面采用智能双向互补增量算法；
  - 本地与云端各自独有的快照自动双向合并互补，两端共有快照按 `updatedAt` 时间戳保留最新版本，双方数据 100% 完整保留不丢失。
- **全自动防抖与一键同步体系**：
  - **常规操作**：顶部状态栏实时指示云端连接状态（未配置/就绪/同步中/异常），工具栏新增 **【🔄 同步】** 快捷按钮（带旋转动效），支持一键双向增量同步；
  - **自动化控制**：新增 **【快照变更时自动同步】** 开关，在快照新增、改名、删除后执行 2 秒防抖自动同步；打开面板时若距离上次同步超过 5 分钟自动静默拉取。
- **专属「☁️ 云同步设置抽屉」与已有 Gist 实时搜索选择**：
  - 提供专属毛玻璃云同步配置抽屉，支持输入 GitHub Personal Access Token 并提供眼睛图标明密文切换；
  - 新增 **「📋 搜索/选择已有 Gist」** 交互弹窗：基于 GitHub API 获取账号下全部 Gist，支持描述/文件名/ID 实时模糊搜索，优先置顶推荐含有快照备份文件的 Gist，点击即一键绑定；
  - 提供 **「🚀 自动创建 Gist」** 功能，一键在 GitHub 上创建专属 Secret Gist 并自动绑定；
  - 提供 **「🔍 测试连接与权限」** 按钮，即时验证 Token 状态并展示用户名与 Gist 数量；
  - 隐藏设置抽屉内的滚动条，保持界面简洁现代，同时支持滚轮顺滑滑动。

---

## [1.2.1] - 2026-08-26

### 🧩 大体积快照分片轮播与乱序扫码聚合还原
- **超限分片二维码生成与 500ms 轮播播放器**：
  - 当快照体积超出标准单张二维码容纳上限（> 2.2 KB）时，智能判定并展示超限友好提示与 **「分片轮播生成」** 入口；
  - 采用标准 `LSM_CHUNK` 分片通信协议，以 500ms 一张的高速节拍循环播放动态二维码序列；
  - 播放器内置当前进度指示条、已播帧数/总分片数统计、播放/暂停开关与前后分片手动切换控制。
- **摄像头实时扫码与多片乱序暂存聚合**：
  - 扫码器支持实时接收并解析分片数据帧，自动暂存并持续保持摄像头开启；
  - 视窗上方动态呈现分片接收 HUD 进度条与实时点阵，支持任意次序、任意方向扫描；
  - 分片集齐后自动拼装校验并一键调起还原抽屉，无需手动干预。

---

## [1.2.0] - 2026-08-26

### 🚀 二维码离线扫码流转与可视化交互升级
- **二维码生成与跨端互传**：
  - 支持单条快照一键生成离线高清二维码，支持一键下载 PNG 图片与复制 JSON 数据；
  - 便于手机与 PC、不同浏览器之间快速扫码流转快照凭据。
- **摄像头实时扫码与多渠道导入**：
  - 基于 WebRTC 与 `jsQR` 引擎实现摄像头实时高帧率视频流扫码；
  - 提供「图片识别二维码」与「本地 JSON 文件解析」双备选通道，支持拖拽文件识别。
- **智能域名匹配预警**：
  - 扫码或导入非当前域名快照时，自动呈现域名隔离安全提醒，保障凭据安全。

---

## [1.1.0] - 2026-08-26

### 🔒 硬件级 Web Crypto 加密与跨设备解密
- **AES-GCM (256-bit) 对称加密**：
  - 采用现代密码学标准 `AES-GCM` + `PBKDF2` 密钥派生算法，保障数据本地与导出安全；
  - 内存敏感数据使用完毕即时安全擦除。
- **跨设备稳定解密**：
  - 统一密钥派生标准，彻底解决跨端跨环境导入无法解密的问题。

---

## [1.0.0] - 2026-08-25

### 🎉 初始版本发布
- 核心功能：Cookie / LocalStorage / SessionStorage 一键捕获与极速恢复；
- 悬浮球交互：自由拖拽吸附、黑/白名单域名过滤、快捷扩展菜单支持。
